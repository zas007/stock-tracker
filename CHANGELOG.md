# 台灣股市三大法人買超追蹤 — 版本修改紀錄

專案腳本：`fetch_and_update.py` + `config.py`（v10 起）
Google Sheets ID：`1DCceOxjew5O4ljeBVTdZ1F9URsvl90k42AAdynaYV9g`

---

## v11.51 — 2026/09/09

### 修正

- **「推薦歷史」舊資料因 429 被永久覆蓋消失（本次事故）**
  * 起因：使用者回報「推薦成效」某次寫入失敗，事後發現「推薦歷史」分頁只剩最新一批資料（同一天的幾筆），先前逐日累積的歷史紀錄全部不見
  * 根因排查：全專案幾乎所有工作表的整表寫回都是「`ws.clear()` 清空 → `ws.update()` 整批寫回」兩步驟。`write_with_retry()` 的重試機制是**整個業務函式重跑**，而不是只重試失敗的那一次 API 呼叫；若 `clear()` 成功、緊接著 `update()` 卻撞到 429，例外會被 `write_with_retry` 抓到並重跑整個函式——但重跑時會重新從 Sheets `get_all_values()` 讀「既有資料」，此時讀到的已經是被 `clear()` 清空的空表，於是這次只把「當批新資料」當成全部內容寫回，清空前累積的舊資料就此永久消失。此事故確切發生點是「推薦歷史」在 `_archive_performance()` 內的 clear+update 之間卡到 429
  * 影響範圍：排查後發現同樣「clear+update」寫法還存在於：推薦成效、歷史紀錄、對照分析、族群熱度、集保快取、快取、融資歷史、融券歷史、成交量歷史、收盤價歷史、重大訊息歷史、警訊，以及 `prepend_block()`（明日關注／每日快照／今日買超排行／今日賣超排行／族群聯動 共用）、`purge_old_rows()`（所有 31 天清舊資料的動作），全部都有同一種「clear 成功但 update 失敗就會資料遺失」的風險，只是「推薦歷史」這次剛好撞上
  * 新邏輯：新增共用函式 `safe_rewrite(ws, values, label)`，取代上述全部 15 處呼叫點。重試範圍縮小到「清空+寫回」這一組動作本身（429 時最多重試 5 次，等待時間遞增），且**完全不重新讀取 Sheets**——固定沿用呼叫端已經在記憶體算好、包含「舊+新」完整內容的 `values` 反覆嘗試寫回。因此即使中途連續撞到 429，最多只是這次執行跑比較久，資料本身不會消失；若重試次數用盡仍失敗，會印出明確警告並回傳 `False`，提示需要重新執行程式或到 Google Sheets 版本記錄復原，不會再靜默吞掉例外造成誤判
  * 這次事故遺失的「推薦歷史」舊資料，程式修正本身無法自動復原，已請使用者自行到 Google Sheets「檔案 → 版本記錄」嘗試找回被覆蓋前的版本

### 備忘

- 版本 v11.49 → v11.50（`每日更新.sh` banner 版號同步）→ v11.51（本次 `safe_rewrite` 修正）
- 換到新 Mac（Apple Silicon）執行時遇到兩個環境問題，跟本次程式改動無關，記錄供之後對照：
  1. `./每日更新.sh` 出現 `Permission denied`：新複製到別台電腦的檔案不會帶執行權限，執行一次 `chmod +x 每日更新.sh` 即可，之後這台電腦都會記得
  2. 執行時出現 `ImportError: dlopen(..._cffi_backend...cpython-39-darwin.so...): incompatible architecture (have 'x86_64', need 'arm64...')`：代表目前這個 Terminal／Python 環境裝的是 Intel(x86_64) 版本的 `cryptography` 等套件，但實際跑在 Apple Silicon(arm64) 上。排查步驟：先用 `arch` 確認終端機是否被設定成用 Rosetta(i386) 開啟，若是需改用原生模式開啟；接著執行 `pip3 install --user --force-reinstall --no-cache-dir cryptography gspread google-auth google-auth-oauthlib` 重新安裝成對應架構的版本

---

## v11.49 — 2026/09/01

### 修正

- **「明日關注」表因 429 導致排名列整列看起來空白（實際是被殘留合併儲存格吃掉）**
  * 舊問題：`_apply_banner_merges()` 每次寫入後會先 `unmerge_cells()` 解除全表舊合併，再重新合併「現在」符合橫幅格式的列；這兩個 API 呼叫各自只 try 一次，遇到 429 就印警告直接放棄，沒有重試機制。若前面族群底色/融資融券等大量寫入已把 Sheets 每分鐘寫入配額用光，導致「解除舊合併」失敗，殘留的舊合併儲存格（可能是欄位數較少的舊版本留下、範圍較窄）會卡在原本的絕對列位置上；「明日關注」用 prepend 方式每天所有列都會位移，殘留合併剛好疊到當天新寫入的資料列時，畫面上會顯示成整列空白（只有沒被舊合併涵蓋到、比較晚新增的欄位如「佔股本比重%」還看得到值），但資料其實有正常寫入
  * `ws.clear()`（`prepend_block()` 每天呼叫）只清儲存格內容、不清合併格式，所以這種殘留一旦卡住不會自己好，會持續錯位下去
  * 新邏輯：
    1. 「解除舊合併」失敗時不再繼續往下嘗試合併新內容，直接跳過本次重新合併並印出 `❌` 明顯警示要求人工檢查，避免新資料疊加在殘留合併範圍上而顯示空白
    2. 只合併「最近 2 天」（`max_merge_days=2`）的橫幅列，更舊的歷史區塊不合併，減少每次全表重算/合併的 API 呼叫量，連帶降低觸發 429 的機率
  * 效果：往後遇到解除合併失敗時，畫面會維持「資料有寫入但沒有合併橫幅」而不是「資料被吃掉看起來空白」，且每次合併範圍縮小，429 機率降低

### 備忘

- 本次修正解決的是「以後不會再發生」；使用者截圖裡當下已經卡住的殘留合併儲存格不會自動修好，需等下次執行寫入「明日關注」的選項（1 或 3）時，`_apply_banner_merges()` 跑到全表解除合併那步、且沒有再撞到 429，才會被清乾淨
- 版本 v11.48 → v11.49

---

## v11.48 — 2026/08/27

### 修正

- **背景/cron 執行時，單一工作表寫入失敗會讓整批（含明日關注、推薦成效等後續步驟）全部崩潰中止**
  * 舊問題：Step 4 寫入迴圈中，某工作表 `write_with_retry` 失敗後會呼叫裸 `input("繼續其他工作表？(Y/n): ")`；cron 背景執行沒有終端輸入，`input()` 收到 EOF 直接噴出未捕捉的 Traceback，導致該次執行後面所有步驟都沒跑到（見 2026/08/26 18:00 log 紀錄：OTC 行情 UTF-8 解碼錯誤 → 對照分析+快照寫入失敗 → input() EOFError 整批中止）
  * 新邏輯：新增 `_confirm_continue()`，比照 `_wait_close()`/`_ask_continue()` 的 threading 逾時模式，有互動終端可手動輸入 y/n，無人操作或背景執行 10 秒後自動預設「繼續」，不會再整批崩潰
  * 效果：單一工作表寫入失敗只會跳過該表，不會拖累後續其他工作表的更新

- **「明日關注」表「外資大台指淨部位」列底色殘留，未被納入清白範圍**
  * 舊問題：`_apply_alert_colors()` 只掃描並清白/上色「⚠️ 大盤警訊：」開頭的列，「外資大台指淨部位：」這一列從未被處理，若曾經沾到底色就會一直殘留，不會隨資料日期更新自動清除
  * 新邏輯：一併掃描「外資大台指淨部位：」列，一律強制清白（此列本身不代表警訊等級，不應帶色）
  * 效果：舊資料區塊不會再出現「外資部位列跟警訊列一起殘留底色」的狀況

- **新增 log.txt 自動清理，避免無限累積**
  * 舊問題：crontab 用 `>> log.txt 2>&1` 純 append，程式從未清過，已累積超過 7000 行、360KB
  * 新邏輯：新增 `_rotate_log()`，以每次執行最先印出的「✅ 已載入 config.py」為區塊分界，只保留最近 14 次完整執行紀錄，整段留或整段刪，不會砍到一半留下殘缺片段。刻意掛在流程「結尾」（跑到 🎉 完成才觸發），只有確認這次執行成功才信任它去動 log 檔案；中途崩潰完全不會觸發清理，崩潰當下的完整紀錄會被保留到下一次成功執行為止，不會在排查問題時反而先被清掉
  * 效果：log.txt 檔案大小維持在合理範圍，且不影響崩潰排查

### 備忘

- **「明日關注」整張表出現大片粉紅底色，經排查與程式邏輯無關**：Google Sheets 的 `clear()`（`prepend_block()` 每天呼叫）只清儲存格內容、不清背景格式，底色跟著儲存格「位置」走、不跟著「內容」走。這片粉紅是很久以前某次操作／舊邏輯遺留下來的殘留格式，逐日 `clear()`+`update()` 從未把它洗掉。已請使用者手動全選「明日關注」工作表 →「格式」→「清除格式」一次性清乾淨；確認清除後只要程式不再主動上色，就不會再自己變回粉紅。經討論決定不額外加防護邏輯（不強制每次都重刷全表格式），先觀察現況即可

- 版本 v11.47 → v11.48

---


## backtest.py v1.7 — 2026/08/25

### 新增

- **佔股本比重% × T+1 勝率 回測切面，對應 fetch_and_update.py v11.47（K21）**
  * 背景：K21「籌碼集中指標擴充」新增的「佔股本比重%」評分因子上線後，需要能驗證這項評分是否真的有效，比照 T39 的做法接進 backtest.py
  * `PERFORMANCE_HEADERS` 新增「佔股本比重%」一欄，跟 fetch_and_update.py 手動保持同步
  * `load_perf_history()` 新增讀取 `shares_pct_real`；`_rebuild_features()` 直接帶出真值（此指標無法重建近似值，只能是真值，v11.47 前的舊資料一律空白，切面會歸類「未知」）
  * **新增切面**：【佔股本比重% × T+1 勝率】，分級門檻跟評分公式 `_score_shares_pct()` 完全一致（≥3.0% / 1.5~3.0% / 0.5~1.5% / <0.5%），可直接對照評分公式是否合理
  * `DETAIL_HEADERS`（回測明細）新增「佔股本比重%」一欄；dry-run 假資料同步補值，已跑過 `_demo_dry_run()` 確認新切面正常輸出、無錯誤
  * 版本 v1.6 → v1.7

---

## v11.47 — 2026/08/25

### 修正

- **「明日關注」表警訊底色不再持續累積，只保留最新一筆上色**
  * 背景：`_apply_alert_colors()` 每次寫入後會重新掃描全表，幫每一筆「⚠️ 大盤警訊：」列依當時等級（紅/黃）上色；此表用 prepend 方式每天累積保留舊資料，天數一多，被上色的警訊列越堆越多，整張表越來越花
  * 改為只有「最新一筆」（掃描到的第一筆，因 prepend 排列，最新永遠在最上面）依等級上色，其餘所有歷史警訊列一律清成白底
  * 效果：往後不論累積多少天資料，表格底色只會標示最新狀態，不會逐日堆疊

### 新增（K21：籌碼集中指標擴充）

- **新增「佔股本比重%」評分因子，並已納入評分（0~+8分）**
  * 背景：既有「籌碼集中度%」= 買超張數/當天成交量，反映的是「當天買盤有多兇」，但同一天買超金額大的通常是大型股，佔股本比重天生就低；新增「佔股本比重%」換一個基期（股本），衡量「這波連續買超，法人到底吃下了多少籌碼」，跟籌碼集中度%互補，不重複計分
  * 資料來源：`fetch_tdcc_data()`（集保 CSV）原本只算「大戶持股%」，其實同一份資料裡各級距股數加總就是股本估計值，過去算完即丟棄；這次額外回傳並保留，不需另打股本 API
  * 「集保快取」工作表新增「總股數」欄（5欄，原4欄），`load_tdcc_cache()`/`update_tdcc_cache()`/`fetch_tdcc_if_needed()` 同步改為 4 元素 tuple；相容舊快取（讀不到總股數時補 0）
  * 新增 `_consec_net_sum()`：加總目前連續買超天數內的三法人合計淨買超張數；`calc_shares_pct()`：計算佔股本比重% = 連續買超累計張數×1000 / 集保總股數 × 100
  * 新增 `_score_shares_pct()` 評分函式（0~+8分），門檻可調：`SHARES_PCT_HIGH`/`MID`/`LOW`（config.py，預設 3.0% / 1.5% / 0.5%），已接進 `score_stock()`、`score_stock_relaxed()`
  * `ANALYSIS_HEADERS` 新增「連續買超累計張數」「佔股本比重%」兩欄；`RECOMMEND_HEADERS`（明日關注）、`PERFORMANCE_HEADERS`（推薦成效/推薦歷史）各新增「佔股本比重%」一欄
  * `config.py` 新增 `SHARES_PCT_HIGH`/`SHARES_PCT_MID`/`SHARES_PCT_LOW` 三個門檻常數
  * **已知限制**：「集保快取」第一次改版後，總股數要等下次集保 CSV 更新（每週五）才會有值，這段過渡期「佔股本比重%」會是空白、評分不加分，屬預期中的正常現象，不是 bug
  * backtest.py 對應回測切面另見上方「backtest.py v1.7」

- 版本 v11.46 → v11.47；`每日更新.sh` banner 同步更新

---

## v11.46 — 2026/08/14


### 調整（refactor）

- **「明日關注」「推薦成效」「推薦歷史」欄位讀寫改用具名 index，取代寫死的 row[N]**
  * 背景：欄位順序改動一直靠人工對照 `RECOMMEND_HEADERS`/`PERFORMANCE_HEADERS` 數 index，過去因此出過 v11.37、v11.43 兩次實際計分/存錯欄位的 bug
  * 新增 `REC_IDX`/`PERF_IDX`（由 headers list 自動產生欄名→index 對照表），以及 `_make_rec_row()`/`_rec_cell()`、`_make_perf_row()`/`_perf_cell()` 存取小工具
  * 改寫範圍：`update_recommendation()` 評分迴圈組列、排名寫入、Top5/觀察組代號比對；`_parse_rec_sheet()`；`update_performance()` 解析明日關注今日 block、T+1~T+5 填值迴圈；`_archive_performance()`
  * 純重構，欄位順序與寫入內容完全不變，只是把 magic number 換成具名讀取，方便之後要調欄位順序時不用整個檔案找 index 改
  * 已用模擬資料跑過 `update_recommendation()` → `update_performance()` 全流程，確認輸出欄位對齊 headers、數值無偏移

- **`ANALYSIS_HEADERS`（build_row 對照分析欄位）同樣改用具名 index**
  * 新增 `ANALYSIS_IDX`、`_make_ana_row()`、`_ana_cell()`，`ANALYSIS_HEADERS` 定義移到 `build_row()` 前面
  * 改寫範圍：`build_row()` 組列、`score_stock()`、`score_stock_relaxed()`、`calc_market_alert()`、`_amp_label()`、`_calc_analysis_rows()`、`update_recommendation()` 讀 all_rows 的迴圈
  * **附帶發現一處註解與實際欄位不符**：`calc_market_alert()` 判斷「融資異常放大」讀的其實是「融資增減(張)」原始數值，不是 `calc_margin_health()` 算出的文字標籤——研判是刻意的（文字標籤沒辦法拿數字門檻比大小），這次只改具名讀取、行為不變，註解已寫清楚，邏輯留給之後確認要不要調整
  * 已用模擬資料跑過 `build_row → score_stock → update_recommendation` 全流程，確認 42 欄逐一對齊 `ANALYSIS_HEADERS`

- **「明日關注」觀察組區塊移除重複的欄位標題列**
  * 背景：主榜標題列印過一次後，緊接著的「⚠️ 高風險觀察組」區塊又印了一次一模一樣的標題，捲動時用不到
  * `update_recommendation()` 組 block 時，觀察組只保留「── ⚠️ 高風險觀察組」分隔行，不再重印 `RECOMMEND_HEADERS`
  * 同步清掉 `_parse_rec_sheet()`、`update_performance()` 今日 block 解析裡「跳過觀察組標題列」的死碼（配合拿掉的標題列一起失效，原本靠「代號欄不是數字」自然跳過即可，不影響解析正確性）
  * 已驗證 `_parse_rec_sheet()` 在少了這行標題後仍正確分辨主榜／觀察組、正確取出代號/名稱/評分/現價

- 版本 v11.45 → v11.46；`每日更新.sh` banner 同步更新

---

## backtest.py v1.6 — 2026/08/14

### 調整（refactor）

- **`load_perf_history()` 改用具名 index（PERF_IDX/`_perf_cell()`）讀取「推薦歷史」，取代寫死的 row[N]**
  * backtest.py 是獨立腳本、不 import fetch_and_update.py，所以自己保留一份 `PERFORMANCE_HEADERS` 順序（註解註明要跟主程式手動同步）
  * 純重構，讀出的欄位與數值完全不變
  * 已用模擬 Google Sheets 回傳資料跑過 `load_perf_history()`，確認每個欄位對應正確
  * 跟 fetch_and_update.py v11.46 同一批處理
  * 版本 v1.5 → v1.6

---

## backtest.py v1.4 — 2026/08/12

### 新增 / 調整

- **回測不再重建近似值，改直接讀 fetch_and_update.py v11.44 存的真值，提升準確度**
  * 背景：`_rebuild_features()` 過去自己從「歷史紀錄」重建「連續天數」「籌碼集中度%」，但定義跟評分實際用的不一樣（連續天數用三大法人合計連續天數，評分用三法人各自連續天數取最大值），數字兜不起來；改成優先直接讀「推薦歷史」新存的真值，只有 v11.44 前的舊資料才 fallback 用舊重建邏輯
  * `load_perf_history()` 新增讀取 `consec_real`/`chip_pct_real`/`chip_lbl_real`/`amp`/`dealer`（索引 [16]~[20]，對應 `PERFORMANCE_HEADERS` v11.44 新增欄位）
  * `_rebuild_features()` 改寫：連續天數、籌碼集中度%/評級優先用真值，並新增 `consec_source`/`chip_source` 標記這筆資料是「真值」還是「重建近似值」，寫進回測明細方便核對
  * `DETAIL_HEADERS`（回測明細）新增「連續天數來源」「籌碼集中度來源」「振幅%」「自營商標記」共4欄
  * **新增兩個切面**：【振幅% × T+1 勝率】（<2% / 2~5% / ≥5%⚡三桶）、【自營商標記/重大訊息 × T+1 勝率】（📢利多標記 / 📢利空標記 / 無標記三類）
  * **已知限制**：「買超加速度」仍是重建近似值（fetch_and_update.py 尚未把這欄存進「推薦歷史」）；「融券趨勢」仍待補（需另打 MI_MARGN API）；「量比」「融資趨勢」尚未接進回測（「明日關注」目前也還沒顯示這兩欄）——皆已記入備忘錄待處理清單，按中/低優先排序處理
  * 版本 v1.3 → v1.4

---

## backtest.py v1.5 — 2026/08/12

### 新增（T39 完成）

- **量比/融資趨勢/融券趨勢三個新切面，直接讀 fetch_and_update.py v11.45 存的真值**
  * 背景：這三個都已經是評分公式的一部分（量比7分、融資趨勢-6~+6分、融券趨勢回補+8/加碼-8），但過去只算分沒往下傳給「明日關注」，backtest.py 也就沒辦法驗證有沒有效；`short_trend` 過去甚至一直寫死 `"TODO"`，備忘錄裡誤記成「需要另外重打 MI_MARGN API 才能重建」——其實 fetch_and_update.py 的 `calc_short_trend()` 早就算好了，只是沒存進「推薦歷史」
  * `load_perf_history()` 新增讀取 `vol_ratio_real`/`margin_trend_real`/`short_trend_real`（索引 [21]~[23]，對應 `PERFORMANCE_HEADERS` v11.45 新增欄位）
  * `_rebuild_features()` 的 `short_trend` 欄位不再寫死 `"TODO"`，改直接讀真值
  * **新增三個切面**：【量比 × T+1 勝率】（<1倍量縮 / 1~2倍 / 2~3倍 / ≥3倍爆量）、【融資趨勢 × T+1 勝率】、【融券趨勢 × T+1 勝率】
  * `DETAIL_HEADERS`（回測明細）新增「量比」「融資趨勢」兩欄（「融券趨勢」欄位本來就有，過去內容都是 TODO，現在會是真實標籤）
  * 已用假資料跑過 `calc_win_rate_matrix()`，三個新切面都能正常輸出（dry-run 測試）
  * **已知限制**：v11.45 之前累積的舊資料沒有這三欄，切面會歸類「未知」，準確度要新資料累積後才有保證
  * 版本 v1.4 → v1.5

---

## v11.45 — 2026/08/12

### fetch_and_update.py（T39 完成）

- **「明日關注」補上量比/融資趨勢/融券趨勢三欄顯示，同步傳進「推薦成效」「推薦歷史」**
  * 背景：這三個欄位早就在 `ANALYSIS_HEADERS`（對照分析）算好（量比[31]、融券趨勢[33]、融資趨勢[39]），也已經是評分公式的一部分，但「明日關注」從沒顯示過，下游的「推薦成效/推薦歷史」自然也沒有，導致 backtest.py 長期無法驗證這三個評分項目實際有沒有效
  * `RECOMMEND_HEADERS`（明日關注）新增 `量比`、`融資趨勢`、`融券趨勢` 三欄（index 16~18）
  * `update_recommendation()` 主榜與觀察組都補上這三個值（讀 `row[31]`/`row[39]`/`row[33]`）
  * `PERFORMANCE_HEADERS`（推薦成效／推薦歷史）同步新增這三欄（index 21~23）
  * `update_performance()` 解析「明日關注」時多讀 r[16]/r[17]/r[18] 一併帶入「推薦成效」新增列
  * `VERSION` → v11.45；`每日更新.sh` banner 同步更新

---

## v11.44 — 2026/08/12

### fetch_and_update.py

- **「推薦成效」「推薦歷史」補齊連續天數/籌碼集中度%/籌碼集中度評級/振幅%/自營商標記五欄，供回測直接讀真值**
  * 背景：這五個欄位其實「明日關注」（`RECOMMEND_HEADERS`）早就有算好的真值（index 4/5/6/11/12），但 `update_performance()` 抄寫到「推薦成效」時漏抄，導致 backtest.py 只能自己從「歷史紀錄」重建近似值，跟評分實際用的定義不一致，準確度打折
  * `PERFORMANCE_HEADERS`（推薦成效／推薦歷史，兩表共用）新增 `連續天數`、`籌碼集中度%`、`籌碼集中度評級`、`振幅%`、`自營商標記` 五欄（附加在最後，index 16~20，不影響既有欄位 index）
  * `update_performance()` 解析「明日關注」時，多讀取 r[4]/r[5]/r[6]/r[11]/r[12] 一併帶入「推薦成效」新增列
  * 「推薦歷史」透過 `_archive_performance()` 原樣搬移欄位，自動繼承新欄位，不需另外修改
  * **已知限制**：v11.44 之前累積的舊「推薦成效/推薦歷史」資料沒有這五欄（會是空值），backtest.py v1.4 對舊資料會自動 fallback 用重建近似值，不會出錯，但準確度只有新資料才有保證
  * `VERSION` → v11.44；`每日更新.sh` banner 同步更新

---

## v11.43 — 2026/08/12

### 修正

- **「明日關注／推薦成效／推薦歷史」融資健康度欄位索引錯位（長期存成原始張數而非標籤）**
  * 舊問題：`update_recommendation()` 組裝推薦列時 `health = row[28]`，但 `ANALYSIS_HEADERS[28]` 是「融資增減(張)」（原始數字，如 -59、13、-129），真正的「融資健康度」文字標籤（✅籌碼乾淨／🟡小幅跟進／⚠️散戶大量跟進／🔴法人不買散戶買）在 `row[30]`
  * 影響範圍：只影響**顯示與存檔**（「明日關注」該欄、以及被抄寫過去的「推薦成效」「推薦歷史」），**不影響評分**——`score_stock()`/`score_stock_relaxed()` 早在 v11.37（T35）就已改用正確的 `row[30]`，25 分的融資健康度算分邏輯本身沒有問題
  * 發現經過：backtest.py【融資健康度 × T+1 勝率】切面跑出來的分桶全是散亂的原始數字（每桶樣本數 1~4 筆），而不是預期的 4 種固定標籤，追查後定位到這個索引錯位
  * 新邏輯：`health = row[30]`
  * 效果：往後新累積的「融資健康度」欄位資料會是正確的標籤文字；**過去所有「融資健康度」歷史資料視為無效**，K23/T35 的融資健康度切面驗證需等這次修正後重新累積資料（建議 2~3 週）才有意義
  * `VERSION` → v11.43；`每日更新.sh` banner 同步更新

---

## backtest.py v1.3 — 2026/08/07

- **【新增】「推薦收盤買 vs 建議買進低點買」勝率比較切面**
  * 對應 fetch_and_update.py v11.42：「推薦歷史」現在會存「建議買進價位/低/高」三欄，這次讓 backtest.py 讀進來並拿去算勝率
  * `load_perf_history()` 新增讀取 `buy_label`/`buy_low`/`buy_high`（索引 [13][14][15]，對應 `PERFORMANCE_HEADERS` v11.42 新增的欄位）
  * `main()` 組裝明細時，額外用 `buy_low` 當進場價（出場價仍用同一組 T+N 收盤，只換進場價這一個變數，其餘條件不變才能公平比較），算出 `t1_pnl_buy`~`t5_pnl_buy`
  * `calc_win_rate_matrix()` 新增切面【★推薦收盤買 vs 建議買進低點買 × T+1~T+5 勝率比較】：同一個 T+N 水平下，並列「推薦收盤買」與「建議買進低點買」兩種進場方式的樣本數/勝率/平均漲跌幅/標準差，兩兩對照即可看出差幾個百分點
  * `DETAIL_HEADERS`（回測明細）新增「建議買進價位」「建議買進低」「建議買進高」「T+1~T+5漲跌%(建議買進)」共8欄，方便逐筆核對
  * **已知限制**：v11.42 之前累積的舊推薦資料沒有這三欄（會是空值），「建議買進低點買」那組的樣本數目前會明顯少於「推薦收盤買」——這是資料還在累積中的正常現象，不是算錯；用小樣本測試驗證過排除邏輯正確（見對話紀錄），但**要等 v11.42 上線後累積至少 2~3 週新資料，樣本數追上，這個切面的勝率數字才有參考價值**（同 T35/T38 邏輯）
  * 版本 v1.2 → v1.3

---

## v11.42 — 2026/08/07

### fetch_and_update.py

- **【新增】「推薦成效」「推薦歷史」同步記錄「推薦買進價位」**
  * 背景：v11.41 只在「明日關注」顯示建議買進價位；使用者想進一步回測「照建議價位買 vs 照推薦日收盤價買」勝率差異多少%，需要把數字也存進追蹤表
  * `_buy_price_label()` 改名為 `_buy_price_info()`，回傳從 `(顯示文字)` 改成 `(顯示文字, 區間低點數字, 區間高點數字)` 三個值——文字給「明日關注」顯示，數字另外存進追蹤表供回測
  * `RECOMMEND_HEADERS`（明日關注）新增 `買進區間低`、`買進區間高` 兩欄（緊接在原本 v11.41 的「推薦買進價位」文字欄後面，index 14/15）
  * `PERFORMANCE_HEADERS`（推薦成效／推薦歷史，兩表共用）新增 `建議買進價位`、`建議買進低`、`建議買進高` 三欄（附加在最後，index 13/14/15，不影響既有 T+1~T+5 等欄位 index）
  * `update_performance()` 解析「明日關注」文字內容時，多讀取新增的 [13][14][15] 三欄一併帶入「推薦成效」新增列
  * 現價已超過建議區間（顯示「⚠️追高」文字）時，低/高點數字欄仍照實填入、不清空——回測時才能算出「當初若照建議價位買現在漲跌%是多少」，不受文字警示影響
  * 「推薦歷史」透過 `_archive_performance()` 原樣搬移欄位，自動繼承新欄位，不需另外修改
  * **待辦提醒**：backtest.py 尚未讀取這幾個新欄位，要看到「勝率高多少%」的實際數字，還需要另外在 backtest.py 加一個新的比較切面（例如「T+N收盤 vs 建議買進低點」的報酬率分布），且需累積至少一批新推薦資料（v11.42上線後才會有數字）才能跑出有意義的統計，已記入備忘錄待處理清單
  * `VERSION` → v11.42；`每日更新.sh` banner 同步更新

---

## v11.41 — 2026/08/07

### fetch_and_update.py

- **【新增】「明日關注」新增「推薦買進價位」欄位**
  * 背景：使用者想在推薦榜多一個「大概該掛多少錢買」的參考，討論後決定用「三大法人加權成本價」＋「5日均線」兩個既有基準組成買進區間，不新增外部資料來源
  * `法人加權成本價`：`build_row()` 裡原本就會算的 `all_wavg`（三大法人 FIFO 扣減賣出後、剩餘部位的實際加權成本），跟「出貨風險」燈號用的是同一份數字，兩欄不會互相矛盾
  * `5日均線價`：`ma5_map` 原本只把文字標記（⬆突破/➡站穩/⬇跌破）存進 `ANALYSIS_HEADERS[34]`，數值本身沒存；這次另外補存數字
  * **為了不影響既有 41 個欄位的 index（過去已經因為欄位錯位炸過好幾次 bug，見 v11.37/38 修正紀錄），新欄位一律「附加在陣列最後」，不插入中間**：`ANALYSIS_HEADERS` 新增 `[40]法人加權成本價`、`[41]5日均線價`
  * 新函式 `_buy_price_label(cost_wavg, ma5_value, close)`：兩個基準都有資料時取 `[較低值, 較高值]` 當區間並各自標註來源（例：`58.2(成本)~61.0(5日)`）；只有一個有資料時退化成單一數字；都沒資料回傳 `"-"`；現價已超過區間上緣時前面加 `⚠️追高` 文字提示，而不是硬塞一個現在達不到的數字
  * `RECOMMEND_HEADERS` 新增「推薦買進價位」欄（主榜、觀察組都有）
  * `VERSION` → v11.41；`每日更新.sh` banner 同步更新

---

## v11.40 — 2026/08/04

### fetch_and_update.py

- **【修正】sheet-only 模式寫「明日關注推薦」crash：`free variable 'amp_map' referenced before assignment`**
  * 舊問題：`amp_map`（振幅%資料）只有在 Step 2 實際打 API 抓行情（`fetch_price_map_batch`）的分支才會被賦值；`_run_analysis`／`_run_recommendation` 這兩個 `main()` 內的巢狀函式把 `amp_map` 當外層變數用，Python 只要偵測到 `main()` 內「任一分支」對 `amp_map` 有賦值就會判定它是 `main()` 的區域變數；sheet-only 模式全程只讀快取、完全不打 API，那些賦值分支一個都沒執行到，內層函式一讀取就是「宣告了但沒賦值」
  * 新邏輯：`main()` 一開始先給 `amp_map = {}` 預設值，之後有實際抓到資料的分支再覆蓋
  * 效果：sheet-only 模式不會再整表寫入失敗；代價是該模式下「振幅%」欄位會是空白（本來就沒有打 API 抓當日高低價，並非本次修正造成的新限制）
  * `VERSION` → v11.40；`每日更新.sh` banner 同步更新

---

## v11.39 — 2026/08/04

### config.py

- **【修正】SECTOR_MAP 資料清理**
  * 「半導體」族群內 `2379`（瑞昱）同族群內重複列了兩次，刪除多餘的一筆
  * 3 組跨族群重複代號 `6669`（緯穎，AI伺服器/光通訊）、`4977`（眾達-KY，光通訊/電源供應）、`5483`（中美晶，HBM/CoWoS封裝/電源供應）補上「也保留」註解（CHANGELOG v11.31 記錄已修過，但實際檔案沒有生效，這次一併補齊）

### fetch_and_update.py

- **【新增】「明日關注」5日均線與籌碼集中度改批次化，減少逐支補抓依賴（備忘錄待處理#29）**
  * 舊問題：`fetch_ma5_batch()` 解析「每日快照」展示用工作表湊歷史收盤價，格式脆弱（依賴「統計截至：」區塊標題），且若當天批次/OTC 抓價本身失敗，快照裡當天那批股票的資料就是缺的、之後也補不回來，逐支補抓命中率因而隨網路狀況浮動（實測 78%~100%）；籌碼集中度依賴當日成交量，同樣受批次/OTC 抓價穩定度影響
  * 新邏輯：新增專用「收盤價歷史」工作表（`update_price_history`／`load_price_history`），仿「成交量歷史」模式，每日寫入 `current_prices` 涵蓋股票的收盤價，保留31天；`fetch_ma5_batch()` 改吃這張表取前8個交易日 ＋ 本次執行的 `current_prices` 直接拿今日收盤，兩者合計仍不足5天才逐支補底；新增 SHEET_OPTIONS 選項 `C`
  * 效果：MA5 不再依賴解析脆弱的展示表；累積約2週資料後，逐支補抓需求應大幅下降；上線初期（前5~9個交易日）逐支補抓比例會跟現在差不多，屬預期中的過渡期

- **【修正】OTC（上櫃）行情抓取加上重試機制（備忘錄待處理#30）**
  * 舊問題：`fetch_price_map_otc()` 沒有重試，偶發無回應/逾時直接回傳空表，是籌碼集中度、現價等資料不穩定的根源之一，且該函式被多處共用（現價／籌碼集中度都吃它），一處失敗全部受影響
  * 新邏輯：比照既有 TWSE batch 的重試模式，最多重試2次（間隔3秒）才回傳空表
  * 效果：改一處，現價、籌碼集中度等所有依賴此函式的資料都受惠，直接緩解籌碼集中度不穩定的根源

- **【新增】「明日關注」表橫幅列（資料日期／外資部位／大盤警訊）合併儲存格，手機閱讀不被截斷**
  * 舊問題：這3條橫幅列的文字都塞在 A 欄，手機版寬度有限，右側被截斷看不到完整內容
  * 新邏輯：新增 `_apply_banner_merges()`，把這3條各自合併成橫跨整列的儲存格（各自獨立合併，不是三條併成一格）；因表格採 prepend 疊放、列位置每天都會位移，比照 `_apply_alert_colors` 的做法，每次寫入後重新掃描全表：先解除目前範圍內所有舊合併，再對「現在」符合橫幅格式的列重新合併
  * 效果：這3條橫幅在手機上會完整換行顯示，不再被截斷在單一欄裡
  * `VERSION` → v11.39；`每日更新.sh` banner 同步更新

---



### fetch_and_update.py

- **【修正】三個評分索引錯位 bug（長期存在，非本次新增，順藤摸瓜查出來的）**
  1. **融資健康度分數（25分）長期失效**：`_score_margin()` 用文字標籤（如「✅ 籌碼乾淨」）查表計分，但 `score_stock()`/`score_stock_relaxed()` 讀的是 `row[29]`（融券餘額，數字），對不到任何 key，永遠回傳0分；正確欄位應為 `row[30]`
  2. **量比分數（7分）連帶失效**：`vr_raw` 讀 `row[30]`，但這個位置實際是融資健康度文字標籤，`float()` 轉型失敗被 try/except 吃掉變成 `None`；正確欄位應為 `row[31]`
  3. **連續天數矩陣（40分）自營商項目算錯**：`consec = max(...)` 的自營商那項讀 `row[13]`（自營商**累計**天數），應為 `row[14]`（自營商**連續**天數）
  * 影響：融資健康度＋量比合計32分過去等同虛設，加上矩陣分潛在的自營商累計/連續天數混淆，評分結果修正後會與過去不同
  * 待確認：過去所有回測結論（含 v11.35 情境降權係數的判斷依據）都是在舊評分邏輯下算出，基準不再適用，需累積新資料後重新檢視（已記入備忘錄待處理事項 #35）

- **【修正】「明日關注」兩組欄位顯示對調（純顯示層，不影響評分/排序結果）**
  * 「籌碼集中度%」「籌碼集中度評級」誤讀成「買超加速度」與「籌碼集中度%數值」，已修正為正確的 `row[25]`／`row[26]`
  * 「振幅%」「自營商標記」兩欄的值互相對調，已修正順序
  * `VERSION` → v11.38；`每日更新.sh` banner 同步更新

---

## v11.37 — 2026/07/30

### fetch_and_update.py

- **【修正】快取陷阱：快取存在但價格是壞資料時，仍被當成有效快取直接沿用**
  * 舊：只要偵測到「今天日期」的快取存在就直接信任，完全不檢查快取內容是否有效；如果快取是在 TWSE 資料還沒公布時存下來的（收盤價/成交量全是0），之後不管重跑幾次都會沿用那份壞掉的舊快取，即使後續「補抓全市場」批次有成功抓到新資料，也只會拿去補快取裡沒有的股票，不會覆蓋已存在但是0的舊值
  * 新：快取命中時，先檢查買超/賣超榜裡有效收盤價（>0）的比例，低於 50% 就判定快取不完整，強制視為快取失效、走完整重新抓取
  * 起因：07/30 發現「今日買超排行」「今日賣超排行」「對照分析」的價格/成交量欄位全部空白，且連帶造成「明日關注」主榜與觀察組雙雙掛零（籌碼集中度算不出來，兩邊過濾條件都通不過）
  * `VERSION` → v11.37；`每日更新.sh` banner 同步更新

---

## v11.36 — 2026/07/29

### fetch_and_update.py

- **【調整】「歷史紀錄」「推薦歷史」「警訊」改為日期降序排列（新資料在最上面）**
  * 舊：三張表都是 append 到最後一列，日期由舊到新（升序）
  * 新：寫入後依日期欄位重新排序（header固定第一列），新的在最上面；歷史累積的舊資料也一併轉換，不需手動處理
  * 「明日關注」等原本就用 prepend 疊放的表不受影響（本來就已是新的在上面）
  * 往後新建工作表，沒特別說明一律採此排序（已記入備忘錄協作規則）

### backtest.py（v1.1 → v1.2）

- **【新增】大盤警訊等級 × T+1勝率 切面**
  * 新增 `load_alert_history()` 讀取「警訊」工作表（v11.34新增），依日期 join 進回測明細，拆出 🔴高風險／🟡觀察／🟢正常 分開統計
  * 用途：直接驗證 v11.35 情境降權係數有沒有效；「警訊」表 v11.34 才有，之前的推薦顯示「未知（v11.34前無資料）」屬正常現象
- **【新增】主榜排名 Top1~5 × T+1勝率 切面**
  * 不更動資料結構，回測時對同一推薦日的主榜股票依評分重新排序算名次，驗證分數的「排序能力」而非只看「有沒有效」
- **【新增】獨立代號數、標準差 兩欄，加進「回測勝率」每一列**
  * 獨立代號數：同一股票連續多天上榜時，看得出樣本數背後實際是幾支不同股票，避免誤判樣本量
  * 標準差：搭配平均漲跌幅一起看，同樣平均值但波動大小可能差很多
- **【調整】「回測明細」改依推薦日降序排列，並補上「組別／主榜排名／大盤警訊等級」三欄**
- 待確認：MAE（最大不利變動）尚未實作，需要 fetch_and_update.py 增加逐日追蹤價格才能做，屬較大改動（已記入備忘錄待處理事項#33）

---

## v11.35 — 2026/07/29

### config.py

- **【新增】情境式降權係數 `ALERT_DAMPEN_FACTOR`**
  * 新增 `ALERT_DAMPEN_FACTOR = {"red": 0.5, "yellow": 0.75, "green": 1.0}`，供 `_score_matrix()` 依大盤警訊等級調整「籌碼集中度×連續天數」矩陣分數

### fetch_and_update.py

- **【調整】大盤警訊🟡/🔴時，自動降權「籌碼集中度×連續天數」矩陣分數（滿分40分）**
  * 舊：`score_stock()`／`score_stock_relaxed()` 不分市況，一律用 `_score_matrix()` 全額計分，籌碼集中度越高、連續天數越長分數越高
  * 新：`_score_matrix()` 新增 `dampen` 參數（預設1.0，不影響既有呼叫端），`score_stock()`／`score_stock_relaxed()` 同步加上 `dampen` 參數並往下傳；`update_recommendation()` 依當次 `alert_level` 從 `ALERT_DAMPEN_FACTOR` 取值算出 `_score_dampen`，🟢正常時=1.0（完全不變），🟡觀察時矩陣分數×0.75，🔴高風險時×0.5；其餘9個評分因子（融資健康度、出貨風險、量比、加速度、5日線、集保、當日金額、動能、融資趨勢）不受影響
  * 依據：`backtest.py --days 30` 回測 2026/07 大跌期間，🔵高度集中（131筆）T+1勝率僅40.5%／平均-0.25%、連續3~5天（46筆）僅30.4%／平均-0.85%，均低於一般水準；且主榜（89筆，嚴格過濾）39.3%／-0.49% 實測輸給觀察組（80筆，放寬過濾）43.8%／+0.53%，顯示這兩項訊號在下跌期間會反轉，過度加權反而讓主榜體質變差
  * 效果：大盤警訊亮燈時，籌碼集中度/連續天數的影響力自動調低，讓融資健康度、出貨風險等其他因子的權重相對浮現；正常行情下評分邏輯與門檻完全不變
  * `VERSION` → v11.35；`每日更新.sh` banner 同步更新
  * 待確認：本次調整依據單一次下跌事件（2026/07）的樣本，屬短期防禦性措施；建議累積下一次同等級警訊事件後，比對降權前後 Top5 的 T+1~T+5 實際表現，驗證 0.5/0.75 這兩個係數是否合理，必要時再微調

---

## v11.34 — 2026/07/29

### config.py

- **【新增】大盤警訊門檻設定**
  * 新增 `ALERT_FUTURES_DELTA_YELLOW/RED`、`ALERT_MARGIN_HEALTH_ABS`、`ALERT_MARGIN_COUNT_YELLOW/RED`、`ALERT_SHIP_RISK_COUNT_YELLOW/RED`、`ALERT_COLOR` 等門檻與底色設定

### fetch_and_update.py

- **【新增】大盤警訊彙總與展示（外資期貨變化／融資異常放大／出貨風險紅燈）**
  * 舊：外資大台指單日變化、融資健康度異常放大、出貨風險🔴高 三項訊號分散在各自欄位，沒有整合成單一警戒等級，大跌時不容易一眼看出當下風險程度
  * 新：新增 `calc_market_alert()` 彙總三項訊號算出 🟢正常/🟡觀察/🔴高風險 等級與摘要文字；`update_recommendation()` 寫入「明日關注」表頂部（跟外資期貨部位同一區塊）；新增獨立「警訊」工作表逐日累積歷史記錄；新增 `_apply_alert_colors()`，因「明日關注」採 prepend 疊放、儲存格底色不會跟著內容移動，故每次寫入後重新掃描全表依當時內容重新上色，避免顏色黏在舊列號上
  * 效果：大跌期間可直接從「明日關注」表頂部或獨立「警訊」表看到當下風險等級與紅/黃底色提示，「警訊」表也為日後做警訊準確度回測留下歷史資料
  * `VERSION` → v11.34；`每日更新.sh` banner 同步更新

### 診斷記錄（本次未變更程式邏輯）

- **每日定時排程（cron）連續多日未執行，log.txt 完全無紀錄**，經排查確認：
  1. `crontab -l` 顯示排程仍存在，手動執行 `fetch_and_update.py` 正常無誤，排除程式本身問題
  2. 查看系統郵件（`cat /var/mail/zzz`）發現每次觸發都留下 `/bin/sh: 130: Bad file descriptor`
  3. 確認是 crontab 檔案本身損毀：兩條新舊排程指令黏在同一行、中間缺少換行，導致舊指令結尾的 `2>&1` 被接續解讀成 `2>&130`（誤判導向不存在的檔案描述符 130），shell 在還沒執行到 Python 前就報錯中止
  * 處置：改用 `EDITOR=nano crontab -e` 清空後重新輸入單一乾淨排程行，並以 `crontab -l | cat -et` 確認結尾只有換行符號、無殘留字元；清空 `/var/mail/zzz` 舊錯誤信
  * 待確認：明日 16:30 排程觸發後需再次檢查 log.txt 是否恢復正常

---

## v11.33 — 2026/07/13

### config.py

- **【新增】獨立的「臨時休市」清單 `TEMP_CLOSURES`（#31）**
  * 新增 `TEMP_CLOSURES = {"20260710"}`，先補入 7/10 颱風假

### fetch_and_update.py

- **【修正】颱風假等臨時休市未被排除，導致 T+1~T+5 對照分析日期錯位一天（#31）**
  * 舊：`_is_trading_day()` 只檢查 `HOLIDAYS`；`HOLIDAYS` 來源是 `fetch_holidays_from_twse` 抓取的 TWSE **官方年度假日行事曆**，只含國定假日，不含颱風假等當天才公告的臨時休市，且 `ensure_holidays_loaded()` 每年只查一次（`_HOLIDAYS_ATTEMPTED`），就算 TWSE 事後補行事曆也不會重查。2026/07/10 颱風假未開盤，但未被排除，導致 `_n_trading_days_after()` 誤把 7/10 算成有效交易日，「推薦成效」中推薦日 07/03～07/09 這 5 筆的 T+1~T+5 對照全部往前多算一天
  * 新：新增 `TEMP_CLOSURES` 集合（config.py，人工維護，不受 `_HOLIDAYS_ATTEMPTED` 限制），`_is_trading_day()` 同時排除 `HOLIDAYS` 與 `TEMP_CLOSURES`；`VERSION` → v11.33
  * 效果：`_n_trading_days_after`、`find_trading_day` 等所有依賴 `_is_trading_day()` 的計算一併修正；日後遇到颱風假等臨時休市，只需在 `TEMP_CLOSURES` 手動補一筆日期即可，不用等 TWSE 官方行事曆更新
  * 待確認：受影響的 5 筆推薦（07/03、06、07、08、09）T+N 目標日期修正後全部落在 07/13，理論上今日重跑會自動補齊；但若 07/13 曾用修正前的舊版程式跑過、導致提前誤判「已過期」搬進「推薦歷史」，則需人工核對該工作表是否有欄位空白

---



### fetch_and_update.py

- **【修正】結尾「按 Enter 關閉」永久卡住，直接關視窗會在 log 留下 EOFError traceback**
  * 舊：全部 9 處 `input("按 Enter 關閉...")` 都是無 timeout 的阻塞等待，跟其他 Step 完成後「5秒後自動繼續」的體驗不一致；使用者若直接關閉終端機視窗（不按 Enter），`input()` 收到 EOF 會噴出未捕捉的 `EOFError` traceback，殘留在 log.txt 中（多次出現於 2026/06/26、2026/07/08 等執行紀錄）
  * 新：新增 `_wait_close(timeout=15)`，比照既有 `_ask_continue()` 的 threading 做法，15 秒無輸入自動關閉；全部 9 處 `input("按 Enter 關閉...")` 統一替換為 `_wait_close()`
  * 效果：不再需要每次手動按 Enter 才能關閉視窗；直接關視窗也不會再產生 EOFError traceback

### 診斷記錄（本次未變更程式）

- **2026/07/08 明日關注 Top5 一度掛零、之後重跑只剩 1 檔**，經 log 比對確認：
  1. 首次執行（16:38）OTC 上櫃行情 API 回傳資料損毀（`UnicodeDecodeError` / `Unterminated string`），導致該次大量候選股缺成交量資料、籌碼集中度算不出來被過濾，Top5 掛零 —— 屬於既有的官方 API 不穩定問題，非本次改動造成
  2. 後續重跑（10:12、13:51）OTC 抓取成功，但 MA5 批次「補底」逐支個股抓取的命中率仍隨當下網路狀況浮動（78% vs 其他日常見的100%），代表 v11.31 只解決了「量比」這一項評分來源的同日不穩定，MA5／籌碼集中度等仍依賴逐支補抓、尚未做歷史批次化
  3. 確認 `score_stock()` 的過濾條件（金融股、ETF、現價>400、當日漲幅>8%、出貨風險🔴高、無籌碼資料、連續天數=0）皆為**硬性排除**、非扣分，合格股票不足5檔時 Top5 本來就會少於5檔（含「⚠️ 高風險觀察組」備案區塊），非 bug
  * 待處理：是否要把 MA5／籌碼集中度也比照 v11.31 的成交量歷史模式做批次化、以及是否加上 OTC API 失敗重試機制，皆列入待處理事項，尚未實作

---

## v11.31 — 2026/07/07

### fetch_and_update.py

- **【新增】量比評分改用「成交量歷史」批次計算，解決同日兩次執行排名不穩定問題（待處理 #28）**
  * 舊邏輯：`fetch_price_map_batch`（STOCK_DAY_ALL 批次抓價，涵蓋約 95% 股票）固定回傳 `volume_ratio=None`，因為該 API 不接受日期參數、無法取得前 10 日歷史；只有逐支補抓（`fetch_stock_day_full`）才能算出真實量比。當某次執行因時間過早導致批次判斷日期不符（`if western != date_str: return {}, {}`）而全面 fallback 到逐支補抓時，幾乎全市場股票的評分基礎會整批改變（None→3分 vs 真實量比1~7分），造成同一天執行兩次「明日關注」Top5 排名不一致
  * 新邏輯：新增 `update_volume_history()` / `load_volume_history()` / `calc_volume_ratio_from_history()`，仿照「融資歷史」模式另建「成交量歷史」工作表，每日寫入 `current_prices` 涵蓋的約 300 檔股票（買超+賣超共6組）成交量，保留 31 天、不受「對照分析」5天過濾規則限制；`build_row()` 新增 `volume_hist` 參數，量比優先用「今日成交量 ÷ 近10天平均（歷史不足時以現有天數平均）」計算，兩條路徑（batch/逐支）改為共用同一份資料源；完全無歷史的新股票才 fallback 回原本的即時值
  * `_calc_analysis_rows()` 載入 `volume_hist` 並傳給 `build_row()`；`update_analysis`（對照分析）與 `_run_recommendation`（明日關注）皆共用此函式，兩入口同步生效
  * `SHEET_OPTIONS` 新增選項 `B) 成交量歷史`，完整執行（選項1）預設自動包含
  * 效果：batch 命中與逐支補抓不再各自產生不同評分基礎，同日重跑排名穩定性提升；新機制上線後約需 10 個交易日資料完整累積才達完整覆蓋率（漸進式補滿，比照 MA5 batch 的先例）

### config.py

- **【修正】「半導體」族群內 `2379`（瑞昱）重複列出**
  * 舊：`2379` 在同一個族群清單中出現兩次（第 286、306 行），純屬複製貼上疏漏
  * 新：移除多餘的一筆
  * 效果：`SECTOR_MAP` 成員總數由 358 → 357，無實質功能影響（僅清理冗餘資料）

- **【調整】3 組跨族群重複代號補上「也保留」註解**
  * 舊：`6669`（緯穎，AI伺服器/光通訊）、`4977`（眾達-KY，光通訊/電源供應）、`5483`（中美晶，HBM/CoWoS封裝/電源供應）在兩個族群中重複出現，但沒有任何註解說明是否為刻意設計，與其他 7 組同類型重複（皆有標註「也保留」/「亦保留」）不一致
  * 新：比照既有慣例，各補上一筆「也保留」註解，明確標示為刻意讓該股票同時觸發兩個族群聯動
  * 效果：目前跨族群重複代號共 10 組，全部都有清楚標註，避免日後誤判為疏漏而誤刪

---

## backtest v1.1 — 2026/06/29

### backtest.py

- **單股回測模式（`--single`）新增**
  * 新增 `load_single_stock_setting(ss)`：讀取「回測設定」工作表（A欄=代號、B欄=備註），工作表不存在時自動建立並填入範例
  * 新增 `run_single_stock_backtest(ss, hist_records, codes_remarks)`：從「歷史紀錄」撈指定股票所有法人買超訊號，進場=買超當日收盤，出場=T+3 收盤 或 法人當日轉賣超（取先到者），呼叫 TWSE STOCK_DAY API 補抓收盤價
  * 新增 `_single_stock_summary(results)`：依股票彙整勝率、平均損益、最佳/最差
  * 新增 `write_single_stock_sheet(ss, results, summary)`：輸出至「單股回測」工作表，上半部彙整摘要，下半部逐筆明細（進出場日/價/原因/損益%/T+1~T+3收盤/法人各方向張數/籌碼集中度）
  * 新增 `main_single(ss, dry_run)`：單股回測主流程，整合上述函式
  * 新增 `curl_get(url)`、`fetch_close_price_single(code, date_str)`、`_next_trading_days(date_str, n)`：價格抓取工具（含 module-level `_price_cache` 避免重複打 API）
  * 新增 `SINGLE_STOCK_SETTING = "回測設定"`、`SINGLE_STOCK_RESULT = "單股回測"` 常數
  * `main()` 新增 `--single` 參數，連線後依旗標分流至 `main_single()` 或原有推薦回測流程
  * 版本從 v1.0（架子版）升至 v1.1
  * 原有 `fetch_close_price` TODO 函式保留，不影響既有推薦回測流程

- **執行方式**
  ```bash
  python3 backtest.py --single          # 單股回測（讀「回測設定」工作表）
  python3 backtest.py --single --dry-run  # dry-run 模式（不寫 Sheets）
  ```

---

## v11.30 — 2026/07/02

### 每日更新.sh

- **修正輸出緩衝卡住問題**
  * 8 處 `python3 fetch_and_update.py` / `python3 backtest.py` 呼叫全部加上 `-u`（unbuffered）參數
  * 原因：v11.29 加入 `| tee -a log.txt` 後，Python 偵測 stdout 非 tty，改為整塊緩衝，導致畫面停在「🚀 完整執行...」不動（實際背景仍在跑，只是輸出被緩衝住，直到緩衝區滿或程式結束才一次噴出）
  * 修正後 Step 1~4/5 進度會即時印出，不再有「假卡住」的情況
  * banner 版號同步至 v11.30

---

## v11.29 — 2026/06/26

### fetch_and_update.py

- **5日融資增減趨勢（新增 #26）**
  * 新增 `update_margin_history(ss, date_str, current_margin)`：每日將融資餘額寫入「融資歷史」工作表（格式：日期/代號/融資餘額(張)），保留 31 天，重跑自動覆蓋
  * 新增 `load_margin_history(ss)`：讀取近期融資餘額，回傳 `{code: [(disp, mb), ...]}` 按日期升序
  * 新增 `calc_margin_trend(margin_hist, code, days=5)`：計算近 5 天融資餘額增減，回傳 (delta, label)；label 格式：`↗ 大增N張`（≥500張）/ `↗ 增N張` / `↘ 大減N張` / `↘ 減N張` / `➡ 持平`
  * 新增 `_score_margin_trend(margin_trend)`：融資趨勢評分 -6 ~ +6 分；大減（≥500張）+6 / 小減 +3 / 小增 -3 / 大增 -6，中性 0
  * `build_row` 新增 `margin_hist` 參數，計算並輸出 `[39] 融資趨勢` 欄
  * `ANALYSIS_HEADERS` 新增 `[39] 融資趨勢`
  * `score_stock` 與 `score_stock_relaxed` 均納入 `_score_margin_trend`（主榜+觀察組同步）
  * `SHEET_OPTIONS` 新增 `A) 融資歷史`（寫入時機與融券歷史相同）
  * `_calc_analysis_rows` 在載入 short_hist 後同步載入 `margin_hist`

- **crontab 修正**
  * 移除多餘的第二條（用 `~` 和裸 `python3`，crontab 環境不展開，會執行失敗）
  * 只保留第一條（完整絕對路徑），log 正常寫入

### 每日更新.sh

- **新增 `tee -a log.txt`**：每個模式（完整/只抓/只寫/Debug）執行時同時輸出到終端機與 log.txt（append）
- **banner 版號同步**：從 v11.5 更新至 v11.28（解決待處理 #20）


---

## v11.28 — 2026/06/25

### fetch_and_update.py

- **TWSE API 格式改版因應（`fetch_price_map_batch` 重寫）**
  * TWSE STOCK_DAY_ALL 回傳格式從 JSON 改為 CSV，欄位順序亦不同
  * 新 CSV 欄位：日期[0] 代號[1] 名稱[2] 成交股數[3] 成交金額[4] 開盤[5] 最高[6] 最低[7] 收盤[8] 漲跌價差[9]
  * 函式改為自動偵測：`日期,` 開頭 → CSV 解析；`{` 開頭 → 舊 JSON 解析（相容）；其他 → 印出前 80 字 debug 後 retry
  * CSV 日期欄為民國年（如 `1150625`），修正為前3碼 +1911 轉西元再比對，解決「batch 不適用」降級 OTC 問題

- **retry 等待時間調整**：STOCK_DAY_ALL 失敗重試間隔從 60 秒降為 3 秒

- **每個 Step 結束後詢問是否繼續（新增 `_ask_continue`）**
  * 顯示格式：`[v11.28] HH:MM:SS  耗時 Xs  Step N/5 XXX 完成。按 [Enter] 繼續，輸入 q 後 Enter 中止（5 秒後自動繼續）...`
  * 包含版號（`VERSION` 變數）、當前時間、耗時
  * 插入點：Step 1（三大法人）、Step 2（個股價格）、Step 3（融資融券）、快取命中補抓全市場後、Step 4（寫入 Sheets）
  * 5 秒無操作自動繼續；輸入 `q` + Enter 可中止

- **執行時間列加上版號**：`執行時間：2026/06/25 15:40  [v11.28]`

- **`update_short_history` 函式定義遺漏修正**：`def` 行不知何時被刪除，只剩 docstring 浮在外，補回函式定義

- **重大訊息改為同時抓今日與前一交易日（`fetch_news_announcements` 修改）**
  * 原本只抓當日，16:30 前今日資料未釋出時容易漏抓
  * 改為對前一交易日與今日各打一次 MOPS API，各自印出命中筆數
  * 前一交易日用 `_is_trading_day` 往回找，週末/假日自動跳過

### backtest.py

- **T+4 / T+5 擴充**
  * `load_perf_history`：新增讀取 t4（col[8]）、t5（col[9]），欄位索引整體後移（group→[10], risk→[11], margin_health→[12]）
  * `DETAIL_HEADERS`：新增「推薦星期」欄、T+4/T+5 的收盤/漲跌%/勝負欄位
  * `write_detail_sheet`：自動計算推薦日是週幾（週一～週五）
  * 主流程：`row` dict 新增 `t4_pnl`、`t5_pnl`

- **勝率矩陣擴充**
  * 新增切面 8：「推薦日星期幾 × T+1 勝率」（週一～週五各自統計）
  * 整體總覽從 T+1/T+2/T+3 擴充至 T+1/T+2/T+3/T+4/T+5
  * 切面總數：8 → 9

- **量比 None 基礎分調整（`_score_volume_ratio` 修正）**
  * 問題：batch（TWSE/OTC）命中時量比回傳 `None`，逐支補抓時才有實際值；兩條路徑評分差最多 6 分（None=2 vs 爆量=7），同一天執行兩次因 batch 命中率不同而推薦排名改變
  * 修正：None 時從 2 分改為 3 分（落在「正常量2分」與「溫和放量4分」之間），兩路徑最大差距從 6 分縮小為 4 分，排名穩定性提升

- **族群熱度排序改為趨勢優先**：第一鍵趨勢（↗升溫 > ➡持平 > ↘降溫），第二鍵熱度分

---

## v11.27 — 2026/06/24

### fetch_and_update.py
- **`fetch_price_map_batch` retry 機制（新增）**：TWSE STOCK_DAY_ALL 失敗時不再直接跳逐支，改為最多重試 2 次（每次間隔 60 秒）；日期不符的情況不重試直接放棄（資料本就不適用）；重試 2 次仍失敗才改逐支補抓

---

## v11.26 — 2026/06/24

### fetch_and_update.py
- **`fetch_news_announcements(date_str)`（新增）**：POST MOPS `ajax_t51sb10`，一次撈全市場當日重大訊息公告；對每筆主旨做關鍵字比對，回傳利多/利空清單（中性略過）
- **`update_news_history(ss, date_str, news_items)`（新增）**：將命中公告寫入「重大訊息歷史」工作表（欄位：日期/代號/名稱/標籤/主旨），保留 31 天，重跑自動覆蓋
- **`load_news_for_codes(ss, codes, date_str, days=3)`（新增）**：讀取近 N 天重大訊息，回傳 `{code: [tag, ...]}` 供明日關注標記使用
- **`update_recommendation` 修改**：推薦計算前預載 `_news_map`；推薦股若近 3 天有命中公告，在「自營商標記」欄附加 `📢利多` / `📢利空`（不影響評分）
- **`SHEET_OPTIONS` 新增**：`0) 重大訊息歷史`
- **`NEWS_KEYWORDS` 全域載入**：`getattr(_cfg, "NEWS_KEYWORDS", {"利多": [], "利空": []})`
- **`fetch_price_map_batch` 修正**：4 個提前 return 從 `return {}` 改為 `return {}, {}`，與正常路徑 tuple 回傳一致，消除潛在 `TypeError`

### config.py
- **`NEWS_KEYWORDS`（新增）**：利多關鍵字 10 組（重大合約、法說會、營收創新高等）、利空關鍵字 12 組（獲利預警、財報重編、停工等）；可自行調整，不需動主程式

---

## v11.24 — 2026/06/23

### fetch_and_update.py
- **`fetch_holidays_from_twse(year)`（新增）**：呼叫 TWSE 假日月曆 API，解析民國年日期，回傳 `set of "YYYYMMDD"`
- **`ensure_holidays_loaded(year)`（新增）**：檢查 `HOLIDAYS` 是否已含指定年度資料；若無則呼叫 API 查詢，並用 regex 寫回 config.py 永久記錄
- **`_is_trading_day(d)`（新增）**：判斷是否為交易日（非週末且不在 `HOLIDAYS`）
- **`_n_trading_days_after`（修正）**：改用 `_is_trading_day`，國定假日不再被計入交易日，修正推薦成效 T+1/T+2/T+3 欄位在連假後對齊錯誤的問題
- **`find_trading_day`（修正）**：改用 `_is_trading_day`，國定假日不再嘗試抓法人資料
- 全域載入：`HOLIDAYS = getattr(_cfg, "HOLIDAYS", set())`

### v11.24 → v11.25 修正（同日）
- **`ensure_holidays_loaded` 無限重查 bug 修正**：加入 `_HOLIDAYS_ATTEMPTED` set，查詢失敗後不再重試，避免 44 筆推薦成效觸發 44+ 次 TWSE API 呼叫
- **移除多餘的跨年預載**：`_n_trading_days_after` 的 `ensure_holidays_loaded(d.year + 1)`、`find_trading_day` 的 `ensure_holidays_loaded(now.year - 1)` 均為不必要呼叫，直接移除；T+3 跨年時 `d.year` 自然觸發正確年度
- **`update_performance` 迴圈前統一 warm up**：從 rows 收集所有涉及年度，一次預載，迴圈內不再觸發查詢

### config.py
- **`HOLIDAYS`（新增）**：新增國定假日 set，預填 2026 年已知休市日（含端午 6/19、6/22）；程式首次跑到新年度時自動查 TWSE 補入，不需手動維護
- **`其他` 族群整理**：16 支移出，只保留漢唐(2404)
  - → PCB：精成科(6191)、廣宇(2328)
  - → 電子代工：瑞軒(2489)
  - → 半導體：全新(2455)
  - → 網通：兆赫(2485)、智易(3596)
  - → 面板：TPK-KY(3673)
  - → 電源供應：飛宏(2457)
  - → 傳產：寶成(9904)、復盛應用(6670)、億豐(8464)
  - → 被動元件（現有）：立隆電(2472)、凱美(2375)
  - → 生技醫療（現有）：康霈(6919)
  - → **新建「LED」族群**：富采(3714)、宏齊(6168)
- **`CODE_NAME_MAP` 補入** 16 支新歸類股票名稱
- 族群總數：21 → 22；CODE_NAME_MAP：168 → 184 筆

---

## v11.23 — 2026/06/22

### fetch_and_update.py
- **`PERFORMANCE_HEADERS` 新增「出貨風險」「融資健康度」兩欄（col[9], col[10]）**
  * 推薦成效寫入時從明日關注工作表 `r[7]`（出貨風險）、`r[8]`（融資健康度）取值
  * 往後 T+3 封存至推薦歷史時會帶入這兩欄，供 backtest.py 切面分析使用
  * 舊資料（45 筆）欄位為空，新切面需等新版跑幾天後才有數值

### config.py
- **IC封測族群新增成員**：精材(3374)、景碩(3189)、頎邦(6147)、南茂(8150)
- **CODE_NAME_MAP 補上**：尖點(8021)、南茂(8150)、精材(3374)
- IC封測族群從 7 支擴充至 11 支

### backtest.py
- **`load_perf_history` 新增讀取**：col[8]=組別、col[9]=出貨風險、col[10]=融資健康度
- **`_rebuild_features` 改從推薦歷史直接取**出貨風險/融資健康度，不再填 "TODO"
- **勝率矩陣新增三個切面**：
  * 出貨風險 × T+1 勝率
  * 融資健康度 × T+1 勝率
  * 主榜 vs 觀察組 × T+1 勝率
- 切面總數：5 → 8

---

## v11.22 — 2026/06/18

### 修正 / 調整

- **`_score_matrix` 大型股補償（★ v11.22）**
  * 問題：大型股（群創、南亞科、台積電等）成交量大，法人買再多張籌碼集中度也天生偏低（<10%），matrix 最多只拿 4~10 分（滿分 40），嚴重低估法人佈局力道
  * 修正：籌碼偏低時，依今日買超金額額外補償：
    - 買超金額 ≥ **1億**：base × 1.5（4→6 / 7→11 / 10→15）
    - 買超金額 ≥ **3億**：base × 2.0（4→8 / 7→14 / 10→20，上限 27 比照中度集中）
  * 高度集中 / 中度集中不受影響
  * `_score_matrix(consec, chip_lbl, today_amount=0)` 加第三參數，兩個呼叫點（`score_stock`、`score_stock_relaxed`）同步更新

### 驗算（2026/06/18 資料）

| 股票 | 買超金額 | matrix 舊 | matrix 新 | 估計總分變化 |
|------|---------|---------|---------|-----------|
| 群創 3481 | 11.0 億 | 4 | 8 | +4（約 44→48） |
| 南亞科 2408 | 61.3 億 | 4 | 8 | +4（觀察組估計 64~69） |

---

## v11.21 — 2026/06/17

### 新增

- **動能評分（`_score_momentum`，-2 ~ +5 分）**
  * 當日漲跌% 納入評分：漲 ≥5% → +5；漲 ≥3% → +4；漲 ≥1.5% → +3；漲 ≥0.5% → +2；平盤 → +1；跌 ≥-1.5% → 0；跌 ≥-3% → -1；跌更多 → -2
  * 目的：法人持續買進 + 當日股價強勢 → 加分，避免過度追高的評分失真

- **振幅% 欄位（`ANALYSIS_HEADERS[38]`）**
  * 公式：`(當日最高 - 當日最低) / 最低 × 100%`
  * 來源：STOCK_DAY_ALL batch `row[5]`（最高）、`row[6]`（最低），原本未解析
  * 振幅 ≥5% 顯示 `⚡5.2%`，提示短線機會與風險並存
  * `fetch_price_map_batch` 回傳改為 tuple `(price_map, amp_map)`，所有呼叫點同步更新（共 6 處）

- **高風險觀察組（明日關注下方）**
  * 主榜被過濾掉的股票（出貨風險🔴、現價 > 400 等）另列「⚠️ 高風險觀察組」前5名
  * 條件放鬆：允許出貨風險🔴/🟡、允許高股價；保留：非ETF、非今日賣超、連續天數 ≥1 天
  * 新增 `score_stock_relaxed(row)`，邏輯同主榜但移除硬過濾門檻
  * 觀察組不與主榜重複（排除已在主榜的代號）

- **推薦成效記錄觀察組**
  * `PERFORMANCE_HEADERS` 新增第 9 欄「組別」（`主榜` / `觀察組`）
  * `_parse_rec_sheet` 和 `update_performance` 今日 block 解析均識別觀察組 header，正確記錄組別
  * 後續回測可用「組別」欄分開計算主榜 vs 觀察組勝率

---

## v11.20 — 2026/06/17

### 修正

- **對照分析全部跳過（根本原因：`r[-1]` 欄位錯位）**
  * v11.18 新增 `r[37]`（今日買超金額）後，過濾邏輯 `r[-1]` 從「最近出現日」變成「今日買超金額（數字）」
  * `_trading_days_diff` 用數字當日期 parse 失敗回傳 999，`> 5` 導致全部被過濾掉
  * 修正：改用明確 index `r[36]`（最近出現日），排序同步修正
  * 此 bug 自 v11.18 起存在；v11.15 因只有 37 欄（`r[-1]` 剛好是 `r[36]`）不受影響

- **歷史紀錄讀取被集保 exception 拖走**
  * `update_analysis` 原本把歷史紀錄讀取與集保 `fetch_tdcc_if_needed` 包在同一個 try/except 裡
  * 集保 CSV 下載失敗時 exception 把 `hist_rows` 一起吃掉，導致 `_calc_analysis_rows` 拿到 None
  * 修正：歷史紀錄讀取移出 try/except，只有集保呼叫包在 try 裡

- **`_calc_analysis_rows` 重複打 API 可能讀到空值**
  * `update_analysis` 已讀好 `hist_rows` 但未傳入，`_calc_analysis_rows` 自己再打一次 `get_all_values()`
  * 兩次請求之間若遇 rate limit 或延遲，第二次讀到空，對照分析被跳過
  * 修正：`_calc_analysis_rows` 加 `hist_rows=None` 參數，有傳入時直接用；兩個呼叫點均傳入已讀好的資料

---

## v11.18 — 2026/06/16

### 新增

- **今日法人買超金額納入評分（`_score_net_amount`，0~8分）**
  * 計算方式：今日三法人買超張數 × 當日均價 × 1000（元）
  * 均價來源：快取中已有的 `avg_price`，不打新 API
  * 分桶：≥5億=8分 / ≥2億=6分 / ≥1億=4分 / ≥3千萬=2分 / 未達=1分
  * `ANALYSIS_HEADERS` 新增 `[37] 今日買超金額`
  * 目的：區分「法人小量試單」vs「法人大力買進」，讓大型股與小型股在同一金額尺度下公平比較

---

## v11.17 — 2026/06/16

### 修正

- **fetch-only（選項2）補上集保 CSV 更新**
  * 原本選 2 只抓資料存快取，不碰集保；選 3 寫 Sheets 時讀到的是舊資料
  * 現在選 2 完成後從歷史紀錄取出所有股票代號，呼叫 `fetch_tdcc_if_needed` 更新集保快取
  * 失敗時印警告但不中斷主流程
  * 完成訊息同步改為「含價格與融資券與集保」

---

## v11.16 — 2026/06/15

### 修正

- **集保 API 改版（`fetch_tdcc_data` 整個重寫）**
  * 舊端點 `/portal/smWeb/qryStockAjax?REQ_OPR=qryStockNo` 已改版，回傳 `{"query":null,"suggestions":[]}` 而非股東結構資料
  * 舊備用端點 `/smWeb/QryStockAjax.do` 已 404
  * 改用 opendata CSV 批次下載：`https://opendata.tdcc.com.tw/getOD.ashx?id=1-5`（全市場，每週五更新）
  * CSV 欄位：資料日期 / 證券代號 / 持股分級(1~15) / 人數 / 股數 / 占集保庫存數比例%
  * 大戶定義：持股分級 ≥ 13（持股 50 張以上）
  * 一次請求取得全市場資料，命中率大幅提升（實測 460/460）

- **`fetch_tdcc_if_needed` 簡化**
  * 舊：先 probe 一支股票試日期，再決定是否全量抓取（兩次請求）
  * 新：直接下載 CSV 比對日期，`weekly_chg` 改由程式計算（新 big_pct - 快取舊 big_pct）

### 效能

- **5日均線改批次抓取（`fetch_ma5_batch`）**
  * 舊：367 支逐支打 STOCK_DAY API + sleep（耗時數分鐘）
  * 新：新增 `fetch_ma5_batch(codes, date_str)`，抓近 9 個交易日的 STOCK_DAY_ALL（上市）+ OTC 批次行情，一次組出全部收盤序列
  * 請求次數：367 次 → 約 5~10 次，速度大幅提升
  * 月初 edge case：自動往前多抓一個月的資料確保 5 日窗口完整
  * 原 `fetch_ma5`（逐支版）保留備用，不刪除

### 待驗證

- `fetch_ma5_batch` 命中率是否與舊版相當（預期 ≥ 174/367）

---

## v11.15 — 2026/06/12

### 新增

- **集保庫存變化（待處理 #17）**
  * 新增 `fetch_tdcc_data(codes)`：打 TDCC API（`/portal/smWeb/qryStockAjax?REQ_OPR=qryStockNo`），抓大戶持股%與週變化
  * 新增 `load_tdcc_cache(ss)` / `update_tdcc_cache(ss, codes, new_data)`：讀寫「集保快取」工作表（欄位：代號 / 大戶% / 週變化 / 集保日期）
  * 新增 `fetch_tdcc_if_needed(ss, codes)`：比對快取最新集保日期，有新資料才重抓，否則使用快取（集保每週五更新）
  * 新增 `_score_tdcc(big_pct, weekly_chg)`：集保大戶評分 -5 ~ +5 分（大戶% 高且增加 → 加分，大幅減少 → 重懲）
  * 對照分析新增「集保大戶」欄 [32]（格式：`72.3%（↑0.8）`），插在「量比」之後
  * `score_stock` 納入集保評分，總分上限維持 100 分
  * API 無回應或週中查詢時自動 fallback 到快取舊值，查不到給 0 分，不中斷流程

- **SECTOR_MAP 族群重整（config.py）**
  * 新增「太陽能」族群：元晶（6443）、聯合再生（3576）
  * 新增「被動元件」族群：國巨（2327，從半導體移出）、大毅（2478）、揚博（2493）
  * 「其他」暫存區 16 支股票全數歸類：
    - 光學元件：大立光（3008）、今國光（6209）、華晶科（3059）
    - 電源供應：群電（6412）
    - 生技醫療：藥華藥（6446）
    - PCB：聯茂（6213）、環科（2413）
    - 電子代工：致伸（4915）、日電貿（3090）
    - 半導體：禾伸堂（3026）、國碩（2406）
    - IC封測：尖點（8021）
  * 族群總數：34 → 35

### 欄位索引異動（ANALYSIS_HEADERS）

| index | 欄位 | 說明 |
|-------|------|------|
| [31] | 量比 | 原 [31] 不變 |
| [32] | 集保大戶 | ★ 新增 |
| [33] | 融券趨勢 | 原 [31] → [33] |
| [34] | 5日線 | 原 [32] → [34] |
| [35] | 相對強弱% | 原 [33] → [35] |
| [36] | 最近出現日 | 原 [34] → [36] |

---

## v11.14 — 2026/06/11

### 修正

- **`fetch_market_index` tables 全空問題**
  * MI_INDEX 在盤中（16:30 前）回傳空 tables，非 API 格式問題
  * 新增 fallback：改抓 FMTQIK（月指數統計），取當日與前日收盤計算漲跌幅
  * MI_INDEX 欄位掃描改為動態找漲跌幅欄（過濾 >20% 的大數字避免誤判成交金額）

---

## v11.13 — 2026/06/11

### 修正

- **`fetch_market_index` aaData key 遺漏**
  * MI_INDEX 部分 table 用 `aaData` 而非 `data` 存資料，改為 `table.get("data") or table.get("aaData")`
  * debug 輸出升級：同時印出 `fields` 和前 2 列內容，方便下次確認

- **`fetch_ma5` 上櫃股票命中率低**
  * `_fetch_stock_day_rows` 改為先打 TWSE，失敗或無資料時自動 fallback 到 TPEX `st43_result.php`
  * TPEX 收盤欄位在 `row[2]`（TWSE 為 `row[6]`），依 source 自動切換
  * 預期命中率從 ~21% 大幅提升

---

*最後更新：2026/06/25（v11.28）*
